window.addEventListener("load", function(){
    const box = document.getElementById("body-login");

    box.style.transition = "opacity 1s";
    box.style.opacity = "1";
});//esto es para darle presentacion al formulario (agregando un opacity transitivo)